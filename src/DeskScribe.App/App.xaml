<Application x:Class="DeskScribe.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:tb="http://www.hardcodet.net/taskbar"
             ShutdownMode="OnExplicitShutdown">

    <Application.Resources>
        <tb:TaskbarIcon x:Key="TrayIcon"
                        IconSource="pack://application:,,,/Resources/app.ico"
                        ToolTipText="DeskScribe">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Show DeskScribe" Click="ShowOverlay_Click"/>
                    <MenuItem Header="Hide DeskScribe" Click="HideOverlay_Click"/>
                    <Separator/>
                    <MenuItem Header="Save (Ctrl+S)" Click="Save_Click"/>
                    <MenuItem Header="Load (Ctrl+O)" Click="Load_Click"/>
                    <Separator/>
                    <MenuItem Header="Exit" Click="Exit_Click"/>
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
    </Application.Resources>

</Application>
